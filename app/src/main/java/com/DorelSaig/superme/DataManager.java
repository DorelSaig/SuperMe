package com.DorelSaig.superme;

import com.DorelSaig.superme.Objects.List;

import java.util.ArrayList;

public class DataManager {

    public static ArrayList<List> generateLists(){
        ArrayList<List> lists = new ArrayList<>();

        lists.add(new List()
        .setTitle("סופר").setItems_Counter(20)
        );

        lists.add(new List()
                .setTitle("הום סנטר").setItems_Counter(2)
        );

        lists.add(new List()
                .setTitle("סופר פארם").setItems_Counter(10)
        );

        lists.add(new List()
                .setTitle("מאפייה").setItems_Counter(1)
        );

        lists.add(new List()
                .setTitle("מכולת").setItems_Counter(5)
        );

        lists.add(new List()
                .setTitle("בגדים").setItems_Counter(3)
        );

        lists.add(new List()
                .setTitle("סופר").setItems_Counter(20)
        );

        lists.add(new List()
                .setTitle("הום סנטר").setItems_Counter(2)
        );

        lists.add(new List()
                .setTitle("סופר פארם").setItems_Counter(10)
        );

        lists.add(new List()
                .setTitle("מאפייה").setItems_Counter(1)
        );

        lists.add(new List()
                .setTitle("מכולת").setItems_Counter(5)
        );

        lists.add(new List()
                .setTitle("בגדים").setItems_Counter(3)
        );

        lists.add(new List()
                .setTitle("סופר").setItems_Counter(20)
        );

        lists.add(new List()
                .setTitle("הום סנטר").setItems_Counter(2)
        );

        lists.add(new List()
                .setTitle("סופר פארם").setItems_Counter(10)
        );

        lists.add(new List()
                .setTitle("מאפייה").setItems_Counter(1)
        );

        lists.add(new List()
                .setTitle("מכולת").setItems_Counter(5)
        );




        lists.add(new List()
                .setTitle("הום סנטר").setItems_Counter(2)
        );

        lists.add(new List()
                .setTitle("סופר פארם").setItems_Counter(10)
        );

        lists.add(new List()
                .setTitle("מאפייה").setItems_Counter(1)
        );


        lists.add(new List()
                .setTitle("בגדים").setItems_Counter(3)
        );

        return lists;
    }

}
